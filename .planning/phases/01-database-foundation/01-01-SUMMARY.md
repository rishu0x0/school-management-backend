---
phase: 01-database-foundation
plan: "01"
subsystem: database
tags: [supabase, postgresql, migrations, rls, schema, pgcrypto]

# Dependency graph
requires: []
provides:
  - All 7 database tables (teachers, refresh_tokens, classes, students, attendance_sessions, attendance_records, generated_reports)
  - All UNIQUE constraints (6 total), CHECK constraints (2 total), and performance indexes (11 total)
  - Deterministic seed data: 2 test teachers (UUIDs 000...001 and 000...002) and 2 test classes (UUIDs 000...010 and 000...020)
  - Reproducible supabase/migrations/20260519000001_create_schema.sql
  - Idempotent supabase/seed.sql for RLS isolation testing in plan 01-02
affects: [01-02-rls-policies, 01-03-triggers, 02-go-auth, 03-go-crud, 04-flutter-auth]

# Tech tracking
tech-stack:
  added: [supabase-cli, postgresql-15, pgcrypto]
  patterns:
    - Single-file DDL migration containing all 7 tables for initial schema
    - UNIQUE INDEX on refresh_tokens(token_hash) instead of plain index — prevents collisions and enables fast lookup
    - NO CASCADE on attendance_sessions.class_id — orphan sessions intentionally preserved for audit trail
    - NO CASCADE on attendance_records.student_id — deleted students remain in history records
    - seed.sql uses ON CONFLICT DO NOTHING for full idempotency

key-files:
  created:
    - supabase/config.toml
    - supabase/migrations/20260519000001_create_schema.sql
    - supabase/seed.sql
  modified: []

key-decisions:
  - "UNIQUE INDEX (not plain index) on refresh_tokens(token_hash) — prevents collision on same token hash from concurrent inserts"
  - "attendance_sessions.class_id has no CASCADE — orphan sessions kept for audit trail even if class deleted"
  - "attendance_records.student_id has no CASCADE — deleted students remain in historical attendance records"
  - "month in generated_reports stored as first day of month DATE (e.g., 2026-04-01 for April) — avoids timezone ambiguity with TIMESTAMPTZ"
  - "seed.sql uses fixed UUIDs (000...001, 000...002) required by plan 01-02 RLS isolation tests"
  - "Docker Desktop is a prerequisite for supabase db reset — plan 01-01 file creation is complete but live db reset requires Docker to be installed"

patterns-established:
  - "Migration naming: YYYYMMDDNNNNNN_description.sql — Supabase CLI applies in timestamp order, never rename after apply"
  - "All seed inserts use ON CONFLICT DO NOTHING — makes db reset fully idempotent"
  - "All FK relationships: teacher as root, cascade deletes propagate down ownership chain"

requirements-completed: [INFRA-03]

# Metrics
duration: 2min
completed: 2026-05-19
---

# Phase 1 Plan 01: Database Schema Summary

**Complete PostgreSQL schema for School Management App — 7 tables, 6 UNIQUE constraints, 2 CHECK constraints, 11 performance indexes, and deterministic seed data in a single reproducible migration**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-19T04:57:47Z
- **Completed:** 2026-05-19T04:59:28Z
- **Tasks:** 3 (files created; live db reset pending Docker install)
- **Files modified:** 3

## Accomplishments

- Created `supabase/migrations/20260519000001_create_schema.sql` with all 7 tables, all constraints, all indexes
- Created `supabase/seed.sql` with two deterministic test teachers and two test classes for RLS isolation testing
- Initialized Supabase project (`supabase init`) generating `supabase/config.toml`
- Verified all 7 table definitions, 6 UNIQUE constraints, 2 CHECK constraints, and 11 indexes via static analysis

## Task Commits

Each task was committed atomically:

1. **Task 1: Initialize Supabase project and create DDL migration** - `690807c` (chore)
2. **Task 2: Create seed.sql with two deterministic test teachers** - `4323e9a` (chore)
3. **Task 3: Verify schema integrity** - Static verification complete (no Docker available for live db reset)

## Files Created/Modified

- `supabase/config.toml` — Supabase CLI project configuration (auto-generated by supabase init)
- `supabase/migrations/20260519000001_create_schema.sql` — Complete DDL for all 7 tables with constraints and indexes
- `supabase/seed.sql` — Two deterministic test teachers + two test classes for RLS isolation tests in plan 01-02

## Decisions Made

- **UNIQUE INDEX on token_hash**: Used `CREATE UNIQUE INDEX` (not plain `CREATE INDEX`) on `refresh_tokens(token_hash)` — enforces uniqueness at DB level, not just application level
- **Audit trail preservation**: `attendance_sessions.class_id` has no `ON DELETE CASCADE` — orphan sessions are intentionally preserved if a class is deleted
- **Student history preservation**: `attendance_records.student_id` has no `ON DELETE CASCADE` — deleted students remain in historical records
- **Fixed seed UUIDs**: Test teacher UUIDs are deterministic (`000...001`, `000...002`) as required by plan 01-02 RLS test suite

## Deviations from Plan

None - plan executed exactly as written. SQL content matches plan specification verbatim.

## Issues Encountered

**Docker Desktop not installed.** The plan requires `supabase start` and `supabase db reset` for live schema verification. Docker Desktop (or a compatible container runtime) is not installed on this machine.

- **Impact:** `supabase db reset` cannot run locally — live verification of schema, constraints, and seed data is blocked
- **Files are correct:** Static analysis confirmed all 7 tables, 6 UNIQUE constraints, 2 CHECK constraints, and 11 indexes are present in the migration file
- **Resolution required:** Install Docker Desktop from https://docs.docker.com/desktop/install/mac-install/ then run `supabase db reset` from the project root to complete live verification

## User Setup Required

**Docker Desktop required for local Supabase.** Before plan 01-02 can run:

1. Install Docker Desktop: https://docs.docker.com/desktop/install/mac-install/
2. Start Docker Desktop
3. From project root, run:
   ```bash
   cd "/Users/rishujain/Desktop/Everything/School Management"
   supabase start
   supabase db reset
   ```
4. Verify output ends with "Finished supabase db reset" — no ERROR lines
5. Confirm tables at http://localhost:54323/project/default/database/tables

## Next Phase Readiness

- Migration file and seed.sql are ready — no SQL changes needed
- Plan 01-02 (RLS policies) can proceed once Docker is installed and `supabase db reset` runs cleanly
- Seed teacher UUIDs (`000...001` and `000...002`) are the exact IDs expected by plan 01-02 RLS isolation tests

## Self-Check: PASSED

- FOUND: supabase/config.toml
- FOUND: supabase/migrations/20260519000001_create_schema.sql
- FOUND: supabase/seed.sql
- FOUND: .planning/phases/01-database-foundation/01-01-SUMMARY.md
- FOUND: commit 690807c (Task 1 — init + DDL migration)
- FOUND: commit 4323e9a (Task 2 — seed.sql)
- FOUND: commit 9a7f892 (metadata — SUMMARY, STATE, ROADMAP)

---
*Phase: 01-database-foundation*
*Completed: 2026-05-19*
